const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..','public');test('Audience keeps only the non-interactive Immersa brand beside M',()=>{const html=fs.readFileSync(path.join(root,'audience','index.html'),'utf8');assert.match(html,/<div class="brand-lockup"><img class="brand-mark"[^>]+alt="Immersa"><\/div>/);assert.doesNotMatch(html,/class="brand-lockup"[^>]+href=/);assert.doesNotMatch(html,/public-code-display\.(?:css|js)/);});
