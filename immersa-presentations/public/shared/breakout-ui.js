(function(root,factory){const api=factory(root||{});if(typeof module==='object'&&module.exports)module.exports=api;else{root.ImmersaBreakoutUi=api;api.autoMount();}})(typeof window!=='undefined'?window:globalThis,function(root){
const ACTIVE=new Set(['running','paused','finished']);
const HOLD_MS=75;
function ensureArt(doc){if(doc.querySelector('link[data-breakout-controls-art]'))return;const link=doc.createElement('link');link.rel='stylesheet';link.href='/shared/breakout-controls-art.css';link.dataset.breakoutControlsArt='1';doc.head.appendChild(link);}
function roleFromPath(path){if(/^\/(?:speaker|presenter)(?:\/|$)/.test(path))return'presenter';if(/^\/stage(?:\/|$)/.test(path))return'stage';if(/^\/(?:screen|viewer)(?:\/|$)/.test(path))return'screen';if(/^\/(?:audience(?:\/|$)|p_[a-z0-9]+$)/i.test(path))return'audience';return'';}
function seconds(ms){return Math.max(0,Math.ceil(Number(ms||0)/1000));}
function actionButton(label,event,klass=''){return'<button type="button" class="breakout-action '+klass+'" data-breakout-event="'+event+'">'+label+'</button>';}
function actions(status){if(status==='running')return actionButton('Pausar','breakout:pause')+actionButton('Cerrar','breakout:close','danger');if(status==='paused')return actionButton('Reanudar','breakout:resume','primary')+actionButton('Cerrar','breakout:close','danger');if(status==='finished')return actionButton('Jugar otra vez','breakout:start','primary')+actionButton('Cerrar','breakout:close','danger');return actionButton('Iniciar partida','breakout:start','primary');}
function controllerMarkup(state){const status=state?.status||'idle';return'<section class="breakout-controller"><div class="breakout-controller-head"><span>Juego colaborativo</span><h3>BREAKOUT</h3><p>Todo el público mueve una sola barra.</p></div><div class="breakout-controller-stats"><div><span>Tiempo</span><strong data-breakout-time>'+seconds(state?.remaining_ms)+' s</strong></div><div><span>Puntos</span><strong data-breakout-score>'+Number(state?.score||0)+'</strong></div><div><span>Bloques</span><strong data-breakout-blocks>'+((state?.blocks||[]).filter(block=>block.active).length)+'</strong></div></div><div class="breakout-controller-status" data-breakout-status>'+(status==='running'?'Partida en curso':status==='paused'?'Partida pausada':status==='finished'?'Partida terminada':'Lista para iniciar')+'</div><div class="breakout-controller-actions" data-breakout-actions>'+actions(status)+'</div></section>';}
function boardMarkup(state,role){const remaining=seconds(state?.remaining_ms);const blocks=(state?.blocks||[]).map(block=>'<i class="breakout-block '+(block.active?'':'is-hit')+'" style="--x:'+block.x+';--y:'+block.y+';--w:'+block.width+';--h:'+block.height+'"></i>').join('');const paddle=state?.paddle||{x:.5,width:.22};const ball=state?.ball||{x:.5,y:.72,radius:.016};return'<section class="breakout-overlay breakout-'+role+' '+(state?.status||'idle')+'"><header><span>BREAKOUT</span><strong>'+Number(state?.score||0)+' pts</strong></header><div class="breakout-countdown '+(remaining<=10?'is-urgent':'')+'">'+remaining+'</div><div class="breakout-board">'+blocks+'<i class="breakout-paddle" style="--x:'+paddle.x+';--w:'+paddle.width+'"></i><i class="breakout-ball" style="--x:'+ball.x+';--y:'+ball.y+';--r:'+ball.radius+'"></i></div>'+(state?.status==='paused'?'<div class="breakout-message">PAUSA</div>':'')+(state?.status==='finished'?'<div class="breakout-message"><strong>TIEMPO</strong><span>'+Number(state?.score||0)+' puntos</span></div>':'')+'</section>';}
function controlImages(direction){return'<span class="breakout-direction-glyph" aria-hidden="true">'+(direction==='left'?'←':'→')+'</span><img class="breakout-control-art is-off" src="/shared/breakout-controls/'+direction+'-off.jpg" alt="" draggable="false"><img class="breakout-control-art is-on" src="/shared/breakout-controls/'+direction+'-on.jpg" alt="" draggable="false">';}
function audienceMarkup(state){return'<section class="breakout-overlay breakout-audience '+(state?.status||'idle')+'"><header><span>BREAKOUT</span><strong data-breakout-audience-score>'+Number(state?.score||0)+' pts</strong><em data-breakout-audience-time>'+seconds(state?.remaining_ms)+' s</em></header><div class="breakout-audience-copy" data-breakout-audience-copy>Mantén presionado para mover</div><div class="breakout-pad"><button type="button" data-breakout-direction="left" aria-label="Mover a la izquierda">'+controlImages('left')+'</button><button type="button" data-breakout-direction="right" aria-label="Mover a la derecha">'+controlImages('right')+'</button></div></section>';}
function create(options={}){const doc=options.document||root.document;const socket=options.socket||root.socket;const role=options.role||roleFromPath(root.location?.pathname||'');if(!doc||!socket||!role)return null;ensureArt(doc);let state={status:'idle',score:0,remaining_ms:60000,blocks:[]};let host=null,renderer=null,controlStatus='',audienceMounted=false,heldDirection='',holdTimer=null,mountTimer=null,destroyed=false;const listeners=[];
function listen(target,name,handler,opts){target.addEventListener(name,handler,opts);listeners.push([target,name,handler,opts]);}
function syncHeld(){if(!host)return;host.querySelectorAll('[data-breakout-direction]').forEach(button=>button.classList.toggle('is-held',button.dataset.breakoutDirection===heldDirection));}
function startHold(direction){heldDirection=direction;socket.emit('breakout:input',{direction});root.clearInterval(holdTimer);holdTimer=root.setInterval(()=>{if(state.status==='running')socket.emit('breakout:input',{direction});},HOLD_MS);syncHeld();}
function stopHold(){heldDirection='';root.clearInterval(holdTimer);holdTimer=null;syncHeld();}
function openController(){const button=role==='presenter'?doc.getElementById('interactionToggle'):doc.getElementById('stageActionsButton');if(button&&button.getAttribute('aria-expanded')!=='true')button.click();}
function setCategories(shell,locked){const close=shell.querySelector('[data-interactions-close]');shell.querySelectorAll('[data-interactions-category]').forEach(button=>{const id=button.dataset.interactionsCategory;button.disabled=locked?id!=='games':id==='contests';if(id==='games'){button.removeAttribute('aria-disabled');button.tabIndex=0;}});if(close)close.hidden=locked;}
function activateGames(shell,locked){shell.dataset.view='games';const home=shell.querySelector('.interactions-shell-home');const content=shell.querySelector('[data-interactions-content-root]');if(home)home.hidden=true;if(content)content.hidden=false;if(renderer)renderer.hidden=false;shell.querySelectorAll('[data-interactions-category]').forEach(button=>button.classList.toggle('is-active',button.dataset.interactionsCategory==='games'));setCategories(shell,locked);}
function ensureController(){const shell=doc.querySelector('.interactions-native-shell');if(!shell)return null;const games=shell.querySelector('[data-interactions-category="games"]');if(games){games.disabled=false;games.removeAttribute('aria-disabled');games.tabIndex=0;}const content=shell.querySelector('[data-interactions-content-root]');if(!content)return null;if(!renderer){renderer=doc.createElement('div');renderer.className='interaction-breakout-renderer';content.appendChild(renderer);listen(shell,'click',event=>{const button=event.target.closest?.('[data-interactions-category]');root.setTimeout(()=>{if(button?.dataset.interactionsCategory==='games')activateGames(shell,false);},0);});listen(renderer,'click',event=>{const button=event.target.closest?.('[data-breakout-event]');if(button)socket.emit(button.dataset.breakoutEvent);});}return shell;}
function patchController(){const status=state.status||'idle';if(!renderer||controlStatus!==status){renderer.innerHTML=controllerMarkup(state);controlStatus=status;return;}const time=renderer.querySelector('[data-breakout-time]');const score=renderer.querySelector('[data-breakout-score]');const blocks=renderer.querySelector('[data-breakout-blocks]');if(time)time.textContent=seconds(state.remaining_ms)+' s';if(score)score.textContent=Number(state.score||0);if(blocks)blocks.textContent=(state.blocks||[]).filter(block=>block.active).length;}
function suppressNativeGesture(event){if(event.target.closest?.('[data-breakout-direction]'))event.preventDefault();}
function ensureOverlay(){if(host)return host;host=doc.createElement('div');host.className='breakout-ui-host';doc.body.appendChild(host);listen(host,'pointerdown',event=>{const button=event.target.closest?.('[data-breakout-direction]');if(!button||state.status!=='running')return;event.preventDefault();button.setPointerCapture?.(event.pointerId);startHold(button.dataset.breakoutDirection);},{passive:false});['pointerup','pointercancel','lostpointercapture'].forEach(name=>listen(host,name,stopHold));['contextmenu','dragstart','selectstart'].forEach(name=>listen(host,name,suppressNativeGesture,{passive:false}));return host;}
function patchAudience(){const overlay=ensureOverlay();if(!audienceMounted){overlay.innerHTML=audienceMarkup(state);audienceMounted=true;}const section=overlay.querySelector('.breakout-audience');const score=overlay.querySelector('[data-breakout-audience-score]');const time=overlay.querySelector('[data-breakout-audience-time]');const copy=overlay.querySelector('[data-breakout-audience-copy]');if(section)section.className='breakout-overlay breakout-audience '+(state.status||'idle');if(score)score.textContent=Number(state.score||0)+' pts';if(time)time.textContent=seconds(state.remaining_ms)+' s';if(copy)copy.textContent=state.status==='paused'?'Partida pausada':state.status==='finished'?'¡Partida terminada!':'Mantén presionado para mover';overlay.hidden=!ACTIVE.has(state.status);if(state.status!=='running')stopHold();else syncHeld();}
function render(){if(role==='presenter'||role==='stage'){if(ACTIVE.has(state.status))openController();const shell=ensureController();patchController();if(shell){if(ACTIVE.has(state.status))activateGames(shell,true);else{setCategories(shell,false);renderer.hidden=shell.dataset.view!=='games';}}return;}if(role==='audience'){patchAudience();return;}const overlay=ensureOverlay();overlay.innerHTML=ACTIVE.has(state.status)?boardMarkup(state,role):'';overlay.hidden=!ACTIVE.has(state.status);}
function handle(next){state=next||state;render();}
function requestState(){socket.emit('breakout:request_state');}
socket.on('breakout:state',handle);socket.on('breakout:closed',handle);socket.on('connect',requestState);if(role==='presenter'||role==='stage')mountTimer=root.setInterval(()=>{if(!destroyed&&!renderer)render();},250);render();requestState();root.setTimeout(requestState,350);return{getState:()=>state,render,destroy(){if(destroyed)return;destroyed=true;stopHold();root.clearInterval(mountTimer);socket.off?.('breakout:state',handle);socket.off?.('breakout:closed',handle);socket.off?.('connect',requestState);listeners.splice(0).forEach(([target,name,handler,opts])=>target.removeEventListener(name,handler,opts));renderer?.remove();host?.remove();}};}
let instance=null,timer=null,attempts=0;function autoMount(){if(instance)return instance;function mount(){const liveSocket=typeof socket!=='undefined'?socket:root.socket;if(!liveSocket||!root.document)return null;instance=create({socket:liveSocket,document:root.document});return instance;}if(mount())return instance;if(!timer)timer=root.setInterval(()=>{attempts+=1;if(mount()||attempts>=80){root.clearInterval(timer);timer=null;}},100);return null;}
return{create,autoMount,roleFromPath,controllerMarkup,boardMarkup,audienceMarkup};
});