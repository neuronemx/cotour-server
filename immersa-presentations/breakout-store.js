const DEFAULT_DURATION_MS=6e4;const COUNTDOWN_MS=4e3;const INPUT_WINDOW_MS=100;const SPEED_STEP_PER_SECOND=.015;const MAX_SPEED_MULTIPLIER=1.9;const CONTROL_ROLES=new Set(['presenter','stage']);const ACTIVE_STATUSES=new Set(['ready','countdown','running','paused']);function clamp(v,min,max){return Math.max(min,Math.min(max,v))}function createGameId(nowMs=Date.now()){return'breakout_'+nowMs.toString(36)+'_'+Math.random().toString(36).slice(2,8)}function createBlocks(columns=7,rows=4){const blocks=[],gap=.012,marginX=.07,top=.09,w=(1-marginX*2-gap*(columns-1))/columns,h=.055;for(let row=0;row<rows;row++)for(let column=0;column<columns;column++)blocks.push({id:`b_${row}_${column}`,x:marginX+column*(w+gap),y:top+row*(h+gap),width:w,height:h,active:true});return blocks}function createInitialState(nowMs,durationMs=DEFAULT_DURATION_MS){return{id:createGameId(nowMs),type:'breakout',status:'idle',duration_ms:durationMs,countdown_ends_at_ms:null,started_at_ms:null,ends_at_ms:null,remaining_ms:durationMs,elapsed_active_ms:0,last_step_at_ms:nowMs,score:0,misses:0,paddle:{x:.5,width:.22,speed:0},ball:{x:.5,y:.72,vx:.22,vy:-.3,radius:.016},blocks:createBlocks(),input:{left:0,right:0,window_started_at_ms:nowMs},last_input_by_audience:new Map,updated_at_ms:nowMs}}class BreakoutStore{constructor(options={}){this.now=options.now||Date.now;this.sessions=new Map;this.inputWindowMs=options.inputWindowMs||INPUT_WINDOW_MS;this.durationMs=options.durationMs||DEFAULT_DURATION_MS}getSession(sessionId){const key=String(sessionId||'');if(!this.sessions.has(key))this.sessions.set(key,createInitialState(this.now(),this.durationMs));return this.sessions.get(key)}hasActive(sessionId){return ACTIVE_STATUSES.has(this.getSession(sessionId).status)}prepare(sessionId,role,nowMs=this.now()){if(!CONTROL_ROLES.has(String(role||'')))return{ok:false,reason:'unauthorized_role'};const game=createInitialState(nowMs,this.durationMs);game.status='ready';this.sessions.set(String(sessionId||''),game);return{ok:true,game:game}}start(sessionId,role,nowMs=this.now()){if(!CONTROL_ROLES.has(String(role||'')))return{ok:false,reason:'unauthorized_role'};let game=this.getSession(sessionId);if(game.status!=='ready'){game=createInitialState(nowMs,this.durationMs);this.sessions.set(String(sessionId||''),game)}game.status='running';game.countdown_ends_at_ms=null;game.started_at_ms=nowMs;game.ends_at_ms=nowMs+game.duration_ms;game.remaining_ms=null;game.elapsed_active_ms=0;game.last_step_at_ms=nowMs;game.ball.x=.5;game.ball.y=.72;game.ball.vx=game.paddle.x<.5?.22:-.22;game.ball.vy=-.3;game.updated_at_ms=nowMs;return{ok:true,game:game}}beginCountdown(sessionId,role,nowMs=this.now()){if(!CONTROL_ROLES.has(String(role||'')))return{ok:false,reason:'unauthorized_role'};let game=this.getSession(sessionId);if(game.status!=='ready'){game=createInitialState(nowMs,this.durationMs);this.sessions.set(String(sessionId||''),game)}game.status='countdown';game.countdown_ends_at_ms=nowMs+COUNTDOWN_MS;game.started_at_ms=null;game.ends_at_ms=null;game.remaining_ms=game.duration_ms;game.elapsed_active_ms=0;game.last_step_at_ms=nowMs;game.ball.x=.5;game.ball.y=.72;game.ball.vx=game.paddle.x<.5?.22:-.22;game.ball.vy=-.3;game.updated_at_ms=nowMs;return{ok:true,game:game}}pause(sessionId,role,nowMs=this.now()){const game=this.getSession(sessionId);if(!CONTROL_ROLES.has(String(role||'')))return{ok:false,reason:'unauthorized_role'};if(game.status!=='running')return{ok:false,reason:'invalid_state'};game.remaining_ms=Math.max(0,game.ends_at_ms-nowMs);game.ends_at_ms=null;game.status='paused';game.last_step_at_ms=nowMs;game.updated_at_ms=nowMs;return{ok:true,game:game}}resume(sessionId,role,nowMs=this.now()){const game=this.getSession(sessionId);if(!CONTROL_ROLES.has(String(role||'')))return{ok:false,reason:'unauthorized_role'};if(game.status!=='paused')return{ok:false,reason:'invalid_state'};game.ends_at_ms=nowMs+Math.max(0,game.remaining_ms||0);game.remaining_ms=null;game.status='running';game.last_step_at_ms=nowMs;game.updated_at_ms=nowMs;return{ok:true,game:game}}close(sessionId,role,nowMs=this.now()){const game=this.getSession(sessionId);if(!CONTROL_ROLES.has(String(role||'')))return{ok:false,reason:'unauthorized_role'};game.status='closed';game.ends_at_ms=null;game.remaining_ms=0;game.updated_at_ms=nowMs;return{ok:true,game:game}}input(sessionId,audienceId,direction,nowMs=this.now()){const game=this.getSession(sessionId);if(game.status!=='running'&&game.status!=='ready')return{ok:false,reason:'game_not_running'};const id=String(audienceId||'');if(!id)return{ok:false,reason:'missing_audience_id'};if(direction!=='left'&&direction!=='right')return{ok:false,reason:'invalid_direction'};const lastAt=game.last_input_by_audience.get(id)||0;if(nowMs-lastAt<60)return{ok:false,reason:'rate_limited'};game.last_input_by_audience.set(id,nowMs);game.input[direction]+=1;return{ok:true}}applyCollectiveInput(game,nowMs){if(nowMs-game.input.window_started_at_ms<this.inputWindowMs)return;const delta=game.input.right-game.input.left;game.paddle.speed=clamp(delta*.252,-.9,.9);game.input.left=0;game.input.right=0;game.input.window_started_at_ms=nowMs}movePaddle(game,dtMs,nowMs){this.applyCollectiveInput(game,nowMs);const dt=clamp(dtMs,0,50)/1e3;game.paddle.x=clamp(game.paddle.x+game.paddle.speed*dt,game.paddle.width/2,1-game.paddle.width/2);return dt}speedMultiplier(game){return Math.min(MAX_SPEED_MULTIPLIER,1+Math.floor(Math.max(0,game.elapsed_active_ms||0)/1e3)*SPEED_STEP_PER_SECOND)}step(sessionId,dtMs=16,nowMs=this.now()){const game=this.getSession(sessionId);if(game.status==='ready'){this.movePaddle(game,dtMs,nowMs);game.updated_at_ms=nowMs;return this.snapshot(sessionId,nowMs)}if(game.status==='countdown'){if(nowMs<game.countdown_ends_at_ms)return this.snapshot(sessionId,nowMs);game.status='running';game.countdown_ends_at_ms=null;game.started_at_ms=nowMs;game.ends_at_ms=nowMs+game.duration_ms;game.remaining_ms=null;game.last_step_at_ms=nowMs;game.updated_at_ms=nowMs;return this.snapshot(sessionId,nowMs)}if(game.status!=='running')return this.snapshot(sessionId,nowMs);if(Number.isFinite(game.ends_at_ms)&&nowMs>=game.ends_at_ms){game.status='finished';game.remaining_ms=0;game.ends_at_ms=null;game.updated_at_ms=nowMs;return this.snapshot(sessionId,nowMs)}const boundedMs=clamp(dtMs,0,50);game.elapsed_active_ms+=boundedMs;game.last_step_at_ms=nowMs;const dt=this.movePaddle(game,boundedMs,nowMs),speed=this.speedMultiplier(game),ball=game.ball;ball.x+=ball.vx*dt*speed;ball.y+=ball.vy*dt*speed;if(ball.x-ball.radius<=0&&ball.vx<0)ball.vx*=-1;if(ball.x+ball.radius>=1&&ball.vx>0)ball.vx*=-1;if(ball.y-ball.radius<=0&&ball.vy<0)ball.vy*=-1;const paddleTop=.9,left=game.paddle.x-game.paddle.width/2,right=game.paddle.x+game.paddle.width/2;if(ball.vy>0&&ball.y+ball.radius>=paddleTop&&ball.y<paddleTop+.04&&ball.x>=left&&ball.x<=right){ball.y=paddleTop-ball.radius;ball.vy=-Math.abs(ball.vy);ball.vx=clamp(ball.vx+(ball.x-game.paddle.x)*.9,-.55,.55)}for(const block of game.blocks){if(!block.active)continue;const hit=ball.x+ball.radius>=block.x&&ball.x-ball.radius<=block.x+block.width&&ball.y+ball.radius>=block.y&&ball.y-ball.radius<=block.y+block.height;if(!hit)continue;block.active=false;game.score+=100;ball.vy*=-1;break}if(ball.y-ball.radius>1){game.misses+=1;ball.x=.5;ball.y=.72;ball.vx=game.misses%2?-.22:.22;ball.vy=-.3}if(!game.blocks.some(block=>block.active)){game.status='finished';game.remaining_ms=Math.max(0,game.ends_at_ms-nowMs);game.ends_at_ms=null}game.updated_at_ms=nowMs;return this.snapshot(sessionId,nowMs)}snapshot(sessionId,nowMs=this.now()){const game=this.getSession(sessionId),remaining=game.status==='running'&&Number.isFinite(game.ends_at_ms)?Math.max(0,game.ends_at_ms-nowMs):Math.max(0,game.remaining_ms||0);return{id:game.id,type:game.type,status:game.status,duration_ms:game.duration_ms,countdown_remaining_ms:game.status==='countdown'?Math.max(0,game.countdown_ends_at_ms-nowMs):0,remaining_ms:remaining,score:game.score,misses:game.misses,speed_multiplier:this.speedMultiplier(game),paddle:{...game.paddle},ball:{...game.ball},blocks:game.blocks.map(block=>({...block})),updated_at_ms:game.updated_at_ms}}}module.exports={BreakoutStore:BreakoutStore,DEFAULT_DURATION_MS:DEFAULT_DURATION_MS,COUNTDOWN_MS:COUNTDOWN_MS,INPUT_WINDOW_MS:INPUT_WINDOW_MS,SPEED_STEP_PER_SECOND:SPEED_STEP_PER_SECOND,MAX_SPEED_MULTIPLIER:MAX_SPEED_MULTIPLIER,CONTROL_ROLES:CONTROL_ROLES,ACTIVE_STATUSES:ACTIVE_STATUSES,createBlocks:createBlocks,createInitialState:createInitialState};
